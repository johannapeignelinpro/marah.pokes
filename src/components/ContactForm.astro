---
---

<section id="contact" class="contact ">
  <div class="container-narrow pa-md">
    <h2 class="text-center">Prendre rendez-vous</h2>
    <p class="contact-intro text-center">
      Intéressé·e par un tatouage handpoke ? Remplissez ce formulaire pour
      discuter de votre projet. Je vous répondrai dans les plus brefs délais.
    </p>

    <form class="contact-form" id="contact-form">
      <!-- Informations personnelles -->
      <!-- Honeypot anti-bot (champ invisible) -->
      <input
        type="text"
        name="website"
        id="website"
        style="display:none !important; position:absolute; left:-9999px;"
        tabindex="-1"
        autocomplete="off"
        aria-hidden="true"
      />

      <div class="form-row">
        <div class="form-group">
          <label for="name">Nom & Prénom *</label>
          <input
            type="text"
            id="name"
            name="name"
            required
            maxlength="100"
            placeholder="Votre nom complet"
          />
          <span class="error-message" id="error-name"></span>
        </div>

        <div class="form-group">
          <label for="email">Email *</label>
          <input
            type="email"
            id="email"
            name="email"
            required
            maxlength="255"
            placeholder="votre@email.com"
          />
          <span class="error-message" id="error-email"></span>
        </div>
      </div>

      <div class="form-row">
        <div class="form-group">
          <label for="phone">Téléphone</label>
          <input
            type="tel"
            id="phone"
            name="phone"
            maxlength="20"
            placeholder="06 12 34 56 78"
          />
          <span class="error-message" id="error-phone"></span>
        </div>

        <div class="form-group">
          <label for="instagram">Profil Instagram</label>
          <input
            type="text"
            id="instagram"
            name="instagram"
            maxlength="50"
            placeholder="@votre_pseudo"
          />
          <span class="error-message" id="error-instagram"></span>
        </div>
      </div>

      <!-- Type de projet -->
      <div class="form-group">
        <label>Type de projet *</label>
        <div class="project-type-options">
          <label class="radio-option">
            <input
              type="radio"
              name="project-type"
              value="flash"
              id="type-flash"
              required
            />
            <span class="radio-label">Flash (dessin pré-réalisé)</span>
          </label>
          <label class="radio-option">
            <input
              type="radio"
              name="project-type"
              value="freehand"
              id="type-freehand"
              required
            />
            <span class="radio-label">Projet Perso</span>
          </label>
        </div>
      </div>

      <!-- ============================== -->
      <!-- SECTION FLASH (conditionnelle) -->
      <!-- ============================== -->
      <div id="section-flash" class="form-section" style="display: none;">
        <h3 class="form-section-title">Votre flash</h3>

        <div class="form-group">
          <label for="flash-photo">Photo du flash souhaité *</label>
          <div class="file-upload-area" id="flash-upload-area">
            <input
              type="file"
              id="flash-photo"
              name="flash-photo"
              accept="image/*"
              class="file-input"
            />
            <label for="flash-photo" class="file-label">
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
                <polyline points="17 8 12 3 7 8"></polyline>
                <line x1="12" y1="3" x2="12" y2="15"></line>
              </svg>
              <span>Cliquez pour choisir la photo du flash</span>
            </label>
            <div id="flash-file-list" class="file-list"></div>
          </div>
          <span class="error-message" id="error-flash-photo"></span>
        </div>

        <div class="form-row">
          <div class="form-group">
            <label for="flash-body-part">Partie du corps *</label>
            <input
              type="text"
              id="flash-body-part"
              name="flash-body-part"
              maxlength="100"
              placeholder="Ex : poignet, cheville, épaule..."
            />
            <span class="error-message" id="error-flash-body-part"></span>
          </div>
          <div class="form-group">
            <label for="flash-size">Taille (cm) *</label>
            <input
              type="text"
              id="flash-size"
              name="flash-size"
              maxlength="50"
              placeholder="Ex : 5 × 5 cm"
            />
            <span class="error-message" id="error-flash-size"></span>
          </div>
        </div>

        <div class="form-group">
          <label for="flash-notes">Demandes complémentaires <span class="optional">(optionnel)</span></label>
          <textarea
            id="flash-notes"
            name="flash-notes"
            maxlength="1000"
            placeholder="Modifications mineures souhaitées, ajouts, précisions..."
            rows="3"
          ></textarea>
        </div>

        <div class="form-group">
          <label for="flash-availability">Disponibilités *</label>
          <textarea
            id="flash-availability"
            name="flash-availability"
            placeholder="Indiquez vos disponibilités (jours, créneaux horaires...)"
            rows="3"
          ></textarea>
          <span class="error-message" id="error-flash-availability"></span>
        </div>
      </div>

      <!-- ================================== -->
      <!-- SECTION PROJET PERSO (conditionnelle) -->
      <!-- ================================== -->
      <div id="section-projet-perso" class="form-section" style="display: none;">
        <h3 class="form-section-title">Votre projet</h3>

        <div class="form-row">
          <div class="form-group">
            <label for="perso-body-part">Partie du corps *</label>
            <input
              type="text"
              id="perso-body-part"
              name="perso-body-part"
              maxlength="100"
              placeholder="Ex : poignet, cheville, épaule..."
            />
            <span class="error-message" id="error-perso-body-part"></span>
          </div>
          <div class="form-group">
            <label for="perso-size">Taille (cm) *</label>
            <input
              type="text"
              id="perso-size"
              name="perso-size"
              maxlength="50"
              placeholder="Ex : 5 × 5 cm"
            />
            <span class="error-message" id="error-perso-size"></span>
          </div>
        </div>

        <!-- Upload de photos inspiration -->
        <div class="form-group">
          <label for="photos">Photos de mon travail qui vous inspirent</label>
          <div class="file-upload-area" id="file-upload-area">
            <input
              type="file"
              id="photos"
              name="photos"
              multiple
              accept="image/*"
              class="file-input"
            />
            <label for="photos" class="file-label">
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
                <polyline points="17 8 12 3 7 8"></polyline>
                <line x1="12" y1="3" x2="12" y2="15"></line>
              </svg>
              <span>Cliquez pour choisir ou glissez vos photos ici</span>
            </label>
            <div id="file-list" class="file-list"></div>
          </div>
        </div>

        <!-- Description du projet -->
        <div class="form-group">
          <label for="project">Décrivez votre projet de tatouage *</label>
          <textarea
            id="project"
            name="project"
            maxlength="2000"
            placeholder=" Les petits détails font les grandes histoires, n’hésitez pas à être exhaustif.ve si vous le souhaitez. Donnez des infos sur l’emplacement, taille approximative souhaitée..."
          ></textarea>
          <span class="error-message" id="error-project"></span>
          <div class="warning-message" id="warning-project" style="display: none;"></div>
        </div>

        <!-- Budget -->
        <div class="form-group">
          <label for="budget">Budget envisagé</label>
          <select id="budget" name="budget">
            <option value="">Sélectionnez une fourchette</option>
            <option value="100-200">100€ - 200€</option>
            <option value="200-300">200€ - 300€</option>
            <option value="300-400">300€ - 400€</option>
            <option value="libre">Ton prix sera le mien</option>
          </select>
        </div>

        <!-- Disponibilités -->
        <div class="form-group">
          <label for="availability">Disponibilités</label>
          <textarea
            id="availability"
            name="availability"
            placeholder="Indiquez vos disponibilités (jours, créneaux horaires...)"
            rows="3"
          ></textarea>
        </div>
      </div>

      <div class="form-actions">
        <button type="submit" class="btn btn-primary">
          Envoyer la demande
        </button>
        <div class="patience-message">
        <p>
          J’accepte de discuter de tout projet pour l’adapter à mon univers. Si cela ne correspond pas, je vous guiderai vers un ou une autre artiste adapté.e.
        </p>
        <br>
        <p>
          La sortie d'aiguille est de 80 euros, ensuite, le prix sera variable en fonction de la taille, de l'emplacement et bien sûr, de la difficulté du projet.
        </p>
        <br>
        <p>
Vos demandes seront traitées dès que possible. Je vous réponds au plus vite afin de discuter des détails de la conception et fixer votre rendez vous. Merci pour votre patience et votre compréhension.
        </p>
      </div>

      <!-- Badge de sécurité -->
      <div class="security-badge">
        Formulaire sécurisé - Vos données sont protégées
      </div>

      <p class="form-note">
        * Champs obligatoires<br />
        <!-- TODO: Configurer le service d'envoi d'email (Formspree, Resend, etc.) -->
        <span class="form-warning"
          >⚠️ Le formulaire n'est pas encore connecté à un service d'envoi
          d'email. Contactez-moi directement sur Instagram pour le moment.</span
        >
      </p>
    </form>

 
</section>

<style>
  .contact {
    background-color: rgba(255, 248, 240, 0.7);
    padding-bottom: 0 !important;
  }

  .contact-intro {
    max-width: 600px;
    margin: 0 auto var(--spacing-lg);
    color: var(--text-secondary);
  }

  .contact-form {
    max-width: 600px;
    margin: 0 auto;
    background-color: rgba(255, 255, 255, 0.5);
    padding: var(--spacing-md);
    border: 1px solid var(--color-sable-light);
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-sm);
  }

  /* Info box */
  .info-box {
    background-color: rgba(186, 154, 118, 0.1);
    border-left: 3px solid var(--color-sable);
    padding: 1rem 1.25rem;
    margin-bottom: var(--spacing-md);
    border-radius: 2px;
  }

  .info-box p {
    margin: 0;
    font-size: 0.95rem;
    line-height: 1.6;
    color: var(--text-primary);
  }

  .info-box strong {
    color: var(--color-sable);
  }

  /* Radio options pour type de projet */
  .project-type-options {
    display: flex;
    gap: var(--spacing-sm);
    flex-wrap: wrap;
  }

  .radio-option {
    flex: 1;
    min-width: 200px;
    display: flex;
    align-items: center;
    padding: 1rem;
    background-color: rgba(255, 255, 255, 0.7);
    border: 2px solid var(--color-sable-light);
    border-radius: 4px;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .radio-option:hover {
    border-color: var(--color-sable);
    background-color: rgba(186, 154, 118, 0.05);
  }

  .radio-option input[type="radio"] {
    margin-right: 0.75rem;
    cursor: pointer;
    accent-color: var(--color-sable);
  }

  .radio-option input[type="radio"]:checked + .radio-label {
    color: var(--color-sable);
    font-weight: 700;
  }

  .radio-option:has(input[type="radio"]:checked) {
    border-color: var(--color-sable);
    background-color: rgba(186, 154, 118, 0.1);
  }

  .radio-label {
    cursor: pointer;
    font-size: 0.95rem;
  }

  /* Field hint */
  .field-hint {
    display: block;
    margin-top: 0.5rem;
    font-size: 0.85rem;
    color: var(--text-light);
    font-style: italic;
  }

  /* Error message */
  .error-message {
    display: block;
    margin-top: 0.5rem;
    font-size: 0.85rem;
    color: #d32f2f;
    font-weight: 500;
  }

  .error-message:empty {
    display: none;
  }

  .form-group.has-error input,
  .form-group.has-error textarea,
  .form-group.has-error select {
    border-color: #d32f2f;
    background-color: rgba(211, 47, 47, 0.05);
  }

  /* Warning message */
  .warning-message {
    display: block;
    margin-top: 0.5rem;
    padding: 0.75rem;
    background-color: rgba(255, 152, 0, 0.1);
    border-left: 3px solid #ff9800;
    font-size: 0.85rem;
    color: #e65100;
    border-radius: 2px;
  }

  .warning-message:empty {
    display: none;
  }

  /* Security badge */
  .security-badge {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    margin-top: var(--spacing-md);
    padding: 0.75rem;
    background-color: rgba(76, 175, 80, 0.1);
    border-radius: 4px;
    font-size: 0.85rem;
    color: #2e7d32;
  }

  .security-badge::before {
    content: '🔒';
    font-size: 1rem;
  }

  /* File upload area */
  .file-upload-area {
    position: relative;
  }

  .file-input {
    display: none;
  }

  .file-label {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    padding: 2rem 1rem;
    background-color: rgba(255, 255, 255, 0.7);
    border: 2px dashed var(--color-sable-light);
    border-radius: 4px;
    cursor: pointer;
    transition: all 0.3s ease;
    text-align: center;
  }

  .file-label:hover {
    border-color: var(--color-sable);
    background-color: rgba(186, 154, 118, 0.05);
  }

  .file-label svg {
    color: var(--color-sable);
  }

  .file-label span {
    font-size: 0.95rem;
    color: var(--text-secondary);
  }

  .file-list {
    margin-top: 0.75rem;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .file-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.5rem 0.75rem;
    background-color: rgba(186, 154, 118, 0.05);
    border-radius: 4px;
    font-size: 0.9rem;
  }

  .file-item-name {
    color: var(--text-secondary);
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .file-item-remove {
    background: none;
    border: none;
    color: var(--color-sable);
    cursor: pointer;
    font-size: 1.2rem;
    padding: 0 0.5rem;
    line-height: 1;
  }

  .file-item-remove:hover {
    color: var(--color-sable-dark);
  }

  /* Message de patience */
  .patience-message {
    margin-top: var(--spacing-md);
    padding: 1.25rem;
    background: linear-gradient(135deg, rgba(186, 154, 118, 0.08) 0%, rgba(186, 154, 118, 0.15) 100%);
    border-radius: 4px;
    text-align: center;
  }

  .patience-message p {
    margin: 0;
    font-size: 0.95rem;
    color: var(--text-secondary);
    font-style: italic;
    line-height: 1.6;
  }

  .form-actions {
    margin-top: var(--spacing-md);
    text-align: center;
  }

  .form-actions .btn {
    min-width: 200px;
  }

  .form-note {
    margin-top: var(--spacing-md);
    font-size: 0.85rem;
    color: var(--text-light);
    text-align: center;
  }

  .form-warning {
    display: block;
    margin-top: 0.5rem;
    color: var(--color-sable);
    font-style: italic;
  }

  /* Sections Flash / Projet Perso */
  .form-section {
    margin-top: var(--spacing-md);
    padding-top: var(--spacing-md);
    border-top: 1px solid var(--color-sable-light);
  }

  .form-section-title {
    font-family: 'Amiri', serif;
    font-size: 1.1rem;
    color: var(--color-sable);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: var(--spacing-md);
  }

  .optional {
    font-weight: 400;
    font-style: italic;
    color: var(--text-light);
    font-size: 0.9em;
  }

  .contact-alt {
    padding-top: var(--spacing-lg);
    border-top: 1px solid var(--color-sable-light);
  }

  .contact-methods {
    display: flex;
    justify-content: center;
    gap: var(--spacing-lg);
    margin-top: var(--spacing-md);
    flex-wrap: wrap;
  }

  .contact-method {
    text-align: center;
  }

  .contact-method strong {
    display: block;
    margin-bottom: 0.5rem;
    color: var(--color-sable);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-size: 0.9rem;
  }

  .contact-method a {
    font-size: 1.1rem;
  }

  @media screen and (max-width: 768px) {
    .form-row {
      grid-template-columns: 1fr;
    }

    .contact-form {
      padding: var(--spacing-sm);
    }

    .project-type-options {
      flex-direction: column;
    }

    .radio-option {
      min-width: 100%;
    }
  }
</style>

<script>
  import {
    validateEmail,
    validatePhone,
    validateInstagram,
    validateTextLength,
    containsURL,
    countURLs,
    validateFiles,
    validateFormData,
    type FormData as SecurityFormData
  } from '../utils/formSecurity';

  // =======================
  // ÉLÉMENTS DU FORMULAIRE
  // =======================
  const form = document.getElementById('contact-form') as HTMLFormElement | null;
  const flashRadio = document.getElementById('type-flash') as HTMLInputElement | null;
  const freehandRadio = document.getElementById('type-freehand') as HTMLInputElement | null;
  const sectionFlash = document.getElementById('section-flash') as HTMLElement | null;
  const sectionProjetPerso = document.getElementById('section-projet-perso') as HTMLElement | null;
  const projectTextarea = document.getElementById('project') as HTMLTextAreaElement | null;
  const honeypotField = document.getElementById('website') as HTMLInputElement | null;

  // =======================
  // AFFICHAGE DES ERREURS
  // =======================
  function showError(fieldId: string, message: string) {
    const errorElement = document.getElementById(`error-${fieldId}`);
    const formGroup = document.getElementById(fieldId)?.closest('.form-group');

    if (errorElement) {
      errorElement.textContent = message;
    }
    if (formGroup) {
      formGroup.classList.add('has-error');
    }
  }

  function clearError(fieldId: string) {
    const errorElement = document.getElementById(`error-${fieldId}`);
    const formGroup = document.getElementById(fieldId)?.closest('.form-group');

    if (errorElement) {
      errorElement.textContent = '';
    }
    if (formGroup) {
      formGroup.classList.remove('has-error');
    }
  }

  function clearAllErrors() {
    const errorMessages = document.querySelectorAll('.error-message');
    errorMessages.forEach(el => el.textContent = '');

    const formGroups = document.querySelectorAll('.form-group.has-error');
    formGroups.forEach(el => el.classList.remove('has-error'));
  }

  // =======================
  // GESTION TYPE DE PROJET
  // =======================
  function updateFormFields() {
    if (!flashRadio || !freehandRadio || !sectionFlash || !sectionProjetPerso) return;

    if (flashRadio.checked) {
      sectionFlash.style.display = 'block';
      sectionProjetPerso.style.display = 'none';
    } else if (freehandRadio.checked) {
      sectionFlash.style.display = 'none';
      sectionProjetPerso.style.display = 'block';
    }
  }

  if (flashRadio && freehandRadio) {
    flashRadio.addEventListener('change', updateFormFields);
    freehandRadio.addEventListener('change', updateFormFields);
  }

  // =======================
  // UPLOAD DE FICHIERS
  // =======================
  const fileInput = document.getElementById('photos') as HTMLInputElement | null;
  const fileUploadArea = document.getElementById('file-upload-area') as HTMLElement | null;
  const fileList = document.getElementById('file-list') as HTMLElement | null;
  let selectedFiles: File[] = [];

  function updateFileList() {
    if (!fileList) return;

    fileList.innerHTML = '';

    if (selectedFiles.length === 0) return;

    selectedFiles.forEach((file, index) => {
      const fileItem = document.createElement('div');
      fileItem.className = 'file-item';

      const fileName = document.createElement('span');
      fileName.className = 'file-item-name';
      fileName.textContent = file.name;

      const removeBtn = document.createElement('button');
      removeBtn.className = 'file-item-remove';
      removeBtn.innerHTML = '×';
      removeBtn.type = 'button';
      removeBtn.onclick = () => {
        selectedFiles.splice(index, 1);
        updateFileList();
      };

      fileItem.appendChild(fileName);
      fileItem.appendChild(removeBtn);
      fileList.appendChild(fileItem);
    });
  }

  if (fileInput) {
    fileInput.addEventListener('change', (e) => {
      const target = e.target as HTMLInputElement;
      if (target.files) {
        const newFiles = Array.from(target.files);
        const validation = validateFiles(newFiles);

        if (!validation.valid) {
          alert(validation.error);
          target.value = '';
          return;
        }

        selectedFiles = newFiles;
        updateFileList();
      }
    });
  }

  // Drag & Drop
  if (fileUploadArea) {
    ['dragenter', 'dragover', 'dragleave', 'drop'].forEach(eventName => {
      fileUploadArea.addEventListener(eventName, (e) => {
        e.preventDefault();
        e.stopPropagation();
      });
    });

    ['dragenter', 'dragover'].forEach(eventName => {
      fileUploadArea.addEventListener(eventName, () => {
        fileUploadArea.style.borderColor = 'var(--color-sable)';
        fileUploadArea.style.backgroundColor = 'rgba(186, 154, 118, 0.1)';
      });
    });

    ['dragleave', 'drop'].forEach(eventName => {
      fileUploadArea.addEventListener(eventName, () => {
        fileUploadArea.style.borderColor = '';
        fileUploadArea.style.backgroundColor = '';
      });
    });

    fileUploadArea.addEventListener('drop', (e) => {
      const dt = (e as DragEvent).dataTransfer;
      if (dt && dt.files) {
        const newFiles = Array.from(dt.files).filter(file => file.type.startsWith('image/'));

        const validation = validateFiles(newFiles);
        if (!validation.valid) {
          alert(validation.error);
          return;
        }

        selectedFiles = [...selectedFiles, ...newFiles];
        updateFileList();
      }
    });
  }

  // =======================
  // VALIDATION EN TEMPS RÉEL
  // =======================

  // Validation email en temps réel
  const emailInput = document.getElementById('email') as HTMLInputElement | null;
  if (emailInput) {
    emailInput.addEventListener('blur', () => {
      const validation = validateEmail(emailInput.value);
      if (!validation.valid && emailInput.value.length > 0) {
        showError('email', validation.error!);
      } else {
        clearError('email');
      }
    });
  }

  // Validation téléphone en temps réel
  const phoneInput = document.getElementById('phone') as HTMLInputElement | null;
  if (phoneInput) {
    phoneInput.addEventListener('blur', () => {
      if (phoneInput.value.length > 0) {
        const validation = validatePhone(phoneInput.value);
        if (!validation.valid) {
          showError('phone', validation.error!);
        } else {
          clearError('phone');
        }
      }
    });
  }

  // Validation Instagram en temps réel
  const instagramInput = document.getElementById('instagram') as HTMLInputElement | null;
  if (instagramInput) {
    instagramInput.addEventListener('blur', () => {
      if (instagramInput.value.length > 0) {
        const validation = validateInstagram(instagramInput.value);
        if (!validation.valid) {
          showError('instagram', validation.error!);
        } else {
          clearError('instagram');
        }
      }
    });
  }

  // Détection d'URLs dans la description
  if (projectTextarea) {
    projectTextarea.addEventListener('blur', () => {
      const warningElement = document.getElementById('warning-project');
      if (containsURL(projectTextarea.value)) {
        const urlCount = countURLs(projectTextarea.value);
        if (warningElement) {
          warningElement.textContent = `⚠️ ${urlCount} lien(s) détecté(s). Ils seront neutralisés pour votre sécurité.`;
          warningElement.style.display = 'block';
        }
      } else {
        if (warningElement) {
          warningElement.style.display = 'none';
        }
      }
    });
  }

  // =======================
  // SOUMISSION DU FORMULAIRE
  // =======================
  if (form) {
    form.addEventListener('submit', async (e) => {
      e.preventDefault();

      clearAllErrors();

      // 1. Vérifier le honeypot (protection anti-bot)
      if (honeypotField && honeypotField.value !== '') {
        console.warn('Spam détecté via honeypot');
        return;
      }

      // 2. Collecter les données
      const formData: SecurityFormData = {
        name: (document.getElementById('name') as HTMLInputElement).value,
        email: (document.getElementById('email') as HTMLInputElement).value,
        phone: (document.getElementById('phone') as HTMLInputElement)?.value,
        instagram: (document.getElementById('instagram') as HTMLInputElement)?.value,
        projectType: flashRadio?.checked ? 'flash' : 'freehand',
        project: projectTextarea?.value || '',
        budget: (document.getElementById('budget') as HTMLSelectElement)?.value,
        availability: (document.getElementById('availability') as HTMLTextAreaElement)?.value,
        honeypot: honeypotField?.value,
      };

      // 3. Validation complète
      const validation = validateFormData(formData);

      if (!validation.valid) {
        // Afficher les erreurs
        Object.entries(validation.errors).forEach(([field, message]) => {
          showError(field, message);
        });

        // Scroll vers la première erreur
        const firstError = document.querySelector('.form-group.has-error');
        if (firstError) {
          firstError.scrollIntoView({ behavior: 'smooth', block: 'center' });
        }

        return;
      }

      // 4. Afficher les avertissements si présents
      if (validation.warnings.length > 0) {
        const warningElement = document.getElementById('warning-project');
        if (warningElement) {
          warningElement.textContent = validation.warnings.join('\n');
          warningElement.style.display = 'block';
        }
      }

      // 5. Envoyer le formulaire
      // TODO: Configurer le service d'envoi d'email (Formspree, Resend, etc.)

      alert(
        "Le formulaire n'est pas encore configuré. Veuillez me contacter sur Instagram @marah.pokes\n\n" +
        "✅ Votre formulaire a été validé avec succès !\n" +
        "🔒 Toutes les vérifications de sécurité sont passées."
      );

      // Exemple d'implémentation avec Formspree :
      /*
      const formDataToSend = new FormData();

      // Ajouter les champs du formulaire
      Object.entries(formData).forEach(([key, value]) => {
        if (value && key !== 'honeypot') {
          formDataToSend.append(key, value);
        }
      });

      // Ajouter les fichiers
      selectedFiles.forEach((file, index) => {
        formDataToSend.append(`photo-${index}`, file);
      });

      try {
        const response = await fetch('https://formspree.io/f/YOUR_FORM_ID', {
          method: 'POST',
          body: formDataToSend,
          headers: {
            'Accept': 'application/json'
          }
        });

        if (response.ok) {
          alert('✅ Merci ! Votre message a bien été envoyé.');
          form.reset();
          selectedFiles = [];
          updateFileList();
          clearAllErrors();
        } else {
          alert('❌ Une erreur est survenue. Veuillez réessayer.');
        }
      } catch (error) {
        console.error('Erreur:', error);
        alert('❌ Une erreur est survenue. Veuillez réessayer.');
      }
      */
    });
  }
</script>
